(() => {
  "use strict";

  const REVIEW_REGIONS = [
    {id:"memory",mapId:"MAP-11",region:"Memory Camp",title:"Adaptive mixed review",description:"Strengthen the weakest and most-due language you have already encountered.",icon:"quick",x:87,y:4},
    {id:"echo",mapId:"MAP-12",region:"Echo Cave",title:"Hands-free audio recall",description:"Hear Bisaya, recall the meaning during a five-second gap, then check the English answer.",icon:"headphones",x:59,y:7},
    {id:"campfire",mapId:"MAP-13",region:"Campfire Review",title:"Final conversation challenge",description:"Complete a ten-part challenge drawing from every curriculum region.",icon:"boss",x:29,y:14}
  ];

  const FINAL_CHALLENGE_ITEMS = [
    {prompt:"Someone asks: ‘Kumusta ka?’ Choose a natural reply.",answers:["Maayo ra ko","Okay ra ko","Maayo man ko"],choices:["Maayo ra ko","Maayong gabii","Walay sapayan"],hint:"Reply with a natural expression meaning that you are fine or okay."},
    {prompt:"Introduce yourself as Alex.",answers:["Ako si Alex","Akong ngalan kay Alex","Ang akong ngalan kay Alex"],choices:["Ako si Alex","Taga-England ko","Gikapoy ko"],hint:"Use Ako si before a personal name."},
    {prompt:"Say: ‘I am from England.’",answers:["Taga-England ko","Taga England ko"],choices:["Taga-England ko","Nagpuyo ko sa England","Asa ka gikan?"],hint:"Use Taga- plus the place, followed by ko."},
    {prompt:"Say: ‘I am tired.’",answers:["Gikapoy ko","Medyo gikapoy ko"],choices:["Gikapoy ko","Gigutom ko","Malipayon ko"],hint:"The experienced-state form begins with gi-."},
    {prompt:"Ask the speaker to repeat, please.",answers:["Usba palihug","Usba, palihug","Palihug usba"],choices:["Usba palihug","Kadiyot lang","Wala ko kahibalo"],hint:"Usba means repeat or do again."},
    {prompt:"Ask: ‘How much is everything?’",answers:["Pila tanan?","Pila tanan"],choices:["Pila tanan?","Tagpila ni?","Naa moy sukli?"],hint:"Tanan means all or everything."},
    {prompt:"Which Cebuano pronoun means ‘we’ and includes the listener?",answers:["Kita"],choices:["Kita","Kami","Sila"],hint:"Kita includes the person being spoken to; kami excludes them."},
    {prompt:"Say: ‘I ate.’",answers:["Nikaon ko","Mikaon ko"],choices:["Nikaon ko","Nagkaon ko","Mokaon ko"],hint:"Choose the completed actor-focused form."},
    {prompt:"Say: ‘At two in the afternoon.’",answers:["Alas dos sa hapon"],choices:["Alas dos sa hapon","Ala una sa hapon","Duha ka oras"],hint:"Use alas before dos, followed by sa hapon."},
    {prompt:"Say naturally: ‘I’ll send the file.’",answers:["I-send lang nako ang file","I-send nako ang file"],choices:["I-send lang nako ang file","Gi-download nako ang report","Busy pa ko"],hint:"Keep the Cebuano transfer-focused i- frame around the English root."}
  ];

  function reviewReadiness(){const practised=practisedItems().length;const activeAudio=typeof handsFreeActiveItems==="function"?handsFreeActiveItems().length:0;const modulesStarted=MODULES.filter(module=>moduleStats(module.id).practised>=2).length;return{practised,activeAudio,modulesStarted,memory:practised>=8,echo:activeAudio>=1,campfire:modulesStarted===MODULES.length};}
  function reviewRegionStatus(id){const readiness=reviewReadiness();if(id==="memory")return readiness.memory?`${Math.min(12,readiness.practised)} adaptive items ready`:`${Math.max(0,8-readiness.practised)} more learned items needed`;if(id==="echo")return readiness.echo?`${readiness.activeAudio} active recall phrase${readiness.activeAudio===1?"":"s"}`:"Learn one phrase to activate";return readiness.campfire?"Final challenge ready":`${readiness.modulesStarted}/${MODULES.length} regions started`;}
  function isReviewRegionReady(id){return Boolean(reviewReadiness()[id]);}

  function startMemoryCamp(){const base=reviewPool();if(!base.length){toast("Complete some lessons before entering Memory Camp.");return;}beginQueueSession(base.slice(0,Math.min(12,base.length)),"memory","Memory Camp");}
  function openEchoCave(){const active=typeof handsFreeActiveItems==="function"?handsFreeActiveItems():[];if(!active.length){toast("Learn at least one phrase before entering Echo Cave.");return;}state.reviewRegions={...(state.reviewRegions||{}),echoVisits:Number(state.reviewRegions?.echoVisits||0)+1};saveState();switchView("audioReview");if(!handsFreeReview.queue.length)buildHandsFreeQueue();renderHandsFreeReview();toast("Echo Cave is ready. Press Play review when you are ready to speak and recall.");}
  function startCampfireReview(){if(!bossReady()){toast("Start at least two items in every curriculum region before attempting Campfire Review.");return;}session={mode:"campfire",label:"Campfire Review",queue:BOSS_ITEMS.map((item,index)=>({...item,id:`campfire_${index}`})),index:0,xp:0,correct:0,answered:0,combo:0,boss:true};switchView("learn");loadBossExercise();}

  BOSS_ITEMS.splice(0,BOSS_ITEMS.length,...FINAL_CHALLENGE_ITEMS);
  bossReady=function bossReadyForCampfire(){return MODULES.every(module=>moduleStats(module.id).practised>=2);};
  loadBossExercise=function loadCampfireExercise(){if(!session||session.index>=session.queue.length){finishSession();return;}const question=session.queue[session.index];currentExercise={item:{id:question.id,module:"boss",origin:"Mixed"},mode:"boss",type:"Campfire conversation",prompt:question.prompt,context:"Choose the most natural answer.",answers:question.answers,hint:question.hint,explanation:`A natural answer is: ${question.answers[0]}`,choices:question.choices,audio:null};selectedChoice=null;renderExercise(currentExercise);const moduleLabel=document.getElementById("lessonModule");if(moduleLabel)moduleLabel.textContent=session.label||"Campfire Review";};

  const baseFinishSession=finishSession;
  finishSession=function finishReviewRegionSession(){const mode=session?.mode;if(mode==="memory"||mode==="campfire"){const reviewRegions={...(state.reviewRegions||{})};if(mode==="memory")reviewRegions.memorySessions=Number(reviewRegions.memorySessions||0)+1;if(mode==="campfire")reviewRegions.campfireAttempts=Number(reviewRegions.campfireAttempts||0)+1;state.reviewRegions=reviewRegions;}baseFinishSession();};

  buildWorldMap=function buildBisayaWorldMap(){
    const unlocked=unlockedModules();
    const current=[...unlocked].reverse().find(module=>moduleStats(module.id).pct<100)||unlocked[unlocked.length-1]||MODULES[0];
    const routePoints=[...MODULES.map(module=>`${MODULE_META[module.id].x*10},${MODULE_META[module.id].y*7.6}`),...REVIEW_REGIONS.map(region=>`${region.x*10},${region.y*7.6}`)].join(" ");
    const art=`<svg class="world-map-art" viewBox="0 0 1000 760" preserveAspectRatio="none" aria-hidden="true"><defs><linearGradient id="sea" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#cdeff0"/><stop offset="1" stop-color="#8dd5d5"/></linearGradient><linearGradient id="land" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#f8e8ad"/><stop offset="1" stop-color="#bfe0a2"/></linearGradient></defs><rect width="1000" height="760" rx="36" fill="url(#sea)"/><path d="M60 650C90 530 160 470 245 480c58 7 100 48 157 24 72-30 75-111 160-118 70-5 101 52 164 35 68-18 84-98 153-120 42-13 78 0 111 22V760H40Z" fill="url(#land)" stroke="#80b982" stroke-width="8"/><path d="M95 704c70-54 114-88 180-112 67-25 93 6 145-29 65-44 81-109 144-135 59-24 101 2 150-33 59-43 77-121 166-174" fill="none" stroke="#fff8d6" stroke-width="22" stroke-linecap="round" opacity=".92"/><polyline points="${routePoints}" fill="none" stroke="#1c746c" stroke-width="8" stroke-linecap="round" stroke-linejoin="round" stroke-dasharray="8 18" opacity=".62"/><path d="M120 595q45-70 90 0M150 590q45-95 100 0" fill="#8bbf76" opacity=".75"/><g fill="#5da66d" opacity=".8"><circle cx="770" cy="488" r="28"/><circle cx="817" cy="470" r="34"/><circle cx="852" cy="506" r="25"/><circle cx="742" cy="526" r="22"/></g><path d="M338 280 392 180l58 100Z" fill="#a86b58"/><path d="M370 218h42l-21-38Z" fill="#ef8354"/><path d="M382 180c18-28 33-38 40-65" fill="none" stroke="#d9e3e9" stroke-width="12" stroke-linecap="round" opacity=".7"/><g fill="#f0c36c" stroke="#a87930" stroke-width="4"><rect x="320" y="420" width="66" height="48" rx="5"/><path d="m313 420 40-32 40 32Z"/><rect x="416" y="435" width="55" height="42" rx="5"/><path d="m409 435 34-28 34 28Z"/></g><g fill="#9b8ac8" opacity=".9"><rect x="784" y="72" width="42" height="95" rx="5"/><rect x="836" y="94" width="34" height="73" rx="5"/><rect x="881" y="55" width="48" height="112" rx="5"/><rect x="940" y="104" width="28" height="63" rx="5"/></g><g fill="#fff" opacity=".42"><circle cx="110" cy="90" r="28"/><circle cx="145" cy="82" r="38"/><circle cx="185" cy="96" r="27"/><circle cx="620" cy="86" r="30"/><circle cx="657" cy="78" r="40"/><circle cx="697" cy="93" r="26"/></g></svg>`;
    const moduleNodes=MODULES.map(module=>{const meta=MODULE_META[module.id],stats=moduleStats(module.id),open=isModuleUnlocked(module),isCurrent=current?.id===module.id,strong=stats.pct>=80;const status=open?(strong?"Strong":isCurrent?"Current mission":`${stats.pct}% mastery`):`${module.unlockAt} MP`;return `<div class="map-stop ${open?"open":"locked"} ${isCurrent?"current":""} ${strong?"strong":""}" style="left:${meta.x}%;top:${meta.y}%" data-module="${module.id}"><button class="map-node" type="button" ${open?"":"disabled"} aria-label="${open?`Practise ${module.title}`:`${module.title} locked until ${module.unlockAt} mastery points`}"><span class="map-node-art">${open?pictogram(module.id):pictogram("lock")}</span><span class="map-node-ring" style="--map-progress:${stats.pct*3.6}deg"></span></button><div class="map-label"><strong>${meta.region}</strong><span>${module.title}</span><small>${status}</small></div></div>`;}).join("");
    const reviewNodes=REVIEW_REGIONS.map(region=>{const ready=isReviewRegionReady(region.id);return `<button class="map-camp review-map-stop ${ready?"ready":"locked"}" type="button" style="left:${region.x}%;top:${region.y}%" data-review-region="${region.id}" ${ready?"":"disabled"} title="${region.region}"><span>${ready?pictogram(region.icon):pictogram("lock")}</span><strong>${region.region}</strong><small>${reviewRegionStatus(region.id)}</small></button>`;}).join("");
    return `<div class="world-map-stage">${art}${moduleNodes}${reviewNodes}<div class="world-map-compass" aria-hidden="true"><b>N</b><span>✦</span></div></div>`;
  };

  attachWorldMapEvents=function attachBisayaWorldMapEvents(host){host.querySelectorAll(".map-stop.open").forEach(stop=>stop.querySelector(".map-node")?.addEventListener("click",()=>startModuleSession(stop.dataset.module)));host.querySelectorAll("[data-review-region]").forEach(button=>button.addEventListener("click",()=>{const id=button.dataset.reviewRegion;if(id==="memory")startMemoryCamp();else if(id==="echo")openEchoCave();else if(id==="campfire")startCampfireReview();}));};

  renderSkillTree=function renderBisayaSkillTree(){const tree=document.getElementById("skillTree");if(!tree)return;tree.className="skill-tree world-map-host full-map-host";const curriculumCards=MODULES.map((module,index)=>{const meta=MODULE_META[module.id],stats=moduleStats(module.id),open=isModuleUnlocked(module);return `<article class="region-index-card ${open?"open":"locked"}"><span class="region-index-art">${open?pictogram(module.id):pictogram("lock")}</span><div><small>REGION ${index+1}</small><strong>${meta.region}</strong><span>${module.title}</span><em>${open?`${stats.practised}/${stats.items} practised · ${stats.pct}% current mastery`:`Unlock at ${module.unlockAt} MP`}</em></div></article>`;}).join("");const reviewCards=REVIEW_REGIONS.map((region,index)=>{const ready=isReviewRegionReady(region.id);return `<article class="region-index-card review-region-index ${ready?"open":"locked"}"><span class="region-index-art">${ready?pictogram(region.icon):pictogram("lock")}</span><div><small>REVIEW REGION ${index+1}</small><strong>${region.region}</strong><span>${region.title}</span><em>${reviewRegionStatus(region.id)}</em><button class="text-btn" type="button" data-review-region="${region.id}" ${ready?"":"disabled"}>${region.id==="echo"?"Open audio review":"Start review"}</button></div></article>`;}).join("");tree.innerHTML=buildWorldMap()+`<div class="region-index">${curriculumCards}${reviewCards}</div>`;attachWorldMapEvents(tree);};

  updateBoss=function updateCampfireChallenge(){const ready=bossReady();const status=document.getElementById("bossStatus");if(status){status.textContent=ready?"Ready":"Locked";status.className=`status-tag ${ready?"ready":""}`;}const button=document.getElementById("startBossBtn");if(button){button.disabled=!ready;button.style.opacity=ready?"1":"0.5";button.textContent="Start Campfire Review";}const note=document.getElementById("bossUnlockNote");if(note)note.textContent=ready?"Pass the ten-region challenge with at least 80% accuracy.":`Unlock requirement: start at least two items in every curriculum region (${reviewReadiness().modulesStarted}/${MODULES.length} ready).`;};

  if(!BADGES.some(badge=>badge.id==="memory_camper")){BADGES.push({id:"memory_camper",icon:"🏕️",name:"Memory Camper",description:"Complete one adaptive Memory Camp session",test:progress=>Number(progress.reviewRegions?.memorySessions||0)>=1},{id:"echo_explorer",icon:"🗣️",name:"Echo Explorer",description:"Open Echo Cave for active audio recall",test:progress=>Number(progress.reviewRegions?.echoVisits||0)>=1},{id:"campfire_champion",icon:"🔥",name:"Campfire Champion",description:"Pass the ten-region final review",test:progress=>Number(progress.bossWins||0)>=1});}

  const style=document.createElement("style");style.textContent=`.review-map-stop{min-width:138px;max-width:170px;transform:translate(-50%,-50%);z-index:8}.review-map-stop.ready{box-shadow:0 12px 30px rgba(15,83,77,.22)}.review-map-stop.locked{opacity:.62;filter:saturate(.65)}.review-region-index button{margin-top:8px}.review-region-index em{display:block}@media(max-width:760px){.review-map-stop{min-width:108px;max-width:128px}.review-map-stop strong{font-size:11px}.review-map-stop small{font-size:9px}}`;document.head.appendChild(style);
  if(typeof updateAll==="function")updateAll();
})();
