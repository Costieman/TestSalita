(() => {
  "use strict";

  // Temporarily disabled. The DOM-rewriting share simplifier caused duplicate,
  // disappearing, and non-functional controls when the underlying share dialog
  // re-rendered. Keep the original, stable sharing implementation untouched.
  window.__salitaUniversalShareSimplifierDisabled = true;
})();
